<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hispeed.mapper.CarOwnerMapper">

    <select id="getCarOwnerInfo" resultType="int">
        select
          count(1)
          FROM olp_carowner_info
    </select>

</mapper>